# RTL — Design Under Test (DUT)

The BIRD DUT and a simple smoke test are provided by the course on EDA Playground:

- https://www.edaplayground.com/x/UtWv

Copy the DUT source (`bird.sv` / design file) into this folder so the testbench
in [`../tb`](../tb) can compile and simulate against it.
